package com.beiramar.beiramar.api.repository;

import com.beiramar.beiramar.api.entity.PacoteDisponivel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PacoteDisponivelRepository extends JpaRepository<PacoteDisponivel, Integer> {
}
